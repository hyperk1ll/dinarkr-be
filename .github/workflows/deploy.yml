name: Deploy Backend

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger VPS Webhook
        run: |
          curl -X POST http://vps1.hype-lab.cloud:3001/deploy/backend \
          -H "x-deploy-token: dinar-cicd-secret-123"
